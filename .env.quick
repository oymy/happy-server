# 快速调试模式 - 使用 SQLite 代替 PostgreSQL
# ⚠️ 仅用于本地快速调试，数据不持久化，重启丢失

# 使用 SQLite（无需 PostgreSQL）
DATABASE_URL=file:./dev.db?connection_limit=1

HANDY_MASTER_SECRET=local-dev-secret-quick-test
PORT=3005

# Redis - 如果本地没有 Redis，可以先注释掉这一行
# 程序会降级使用内存存储
REDIS_URL=redis://localhost:6379

# MinIO - 快速调试可以不配置
S3_HOST=localhost
S3_PORT=9000
S3_USE_SSL=false
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=happy
S3_PUBLIC_URL=http://localhost:9000/happy

NODE_ENV=development
DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING=true
