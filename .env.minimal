# 极简配置 - 仅用于本地调试消息功能
# 不使用 MinIO，文件上传功能会失败，但消息功能正常

DATABASE_URL=postgresql://localhost:5432/happy
HANDY_MASTER_SECRET=local-dev-secret-key-for-testing-only
PORT=3005

# Redis
REDIS_URL=redis://localhost:6379

# 禁用 S3（文件上传会失败，但消息功能正常）
# 如果需要文件功能，请启动 MinIO 或使用真实的 S3
S3_HOST=localhost
S3_PORT=9000
S3_USE_SSL=false
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=happy
S3_PUBLIC_URL=http://localhost:9000/happy

# 开发环境
NODE_ENV=development

# 调试日志
DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING=true
